"""Market structure adapters: vol indices, FX, yields, and per-symbol facades."""

from gamma_squeeze.data_sources.structure import (  # noqa: F401
    fx,
    symbols,
    vol_indices,
    yields,
)
